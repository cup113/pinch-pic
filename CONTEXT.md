# pinch-pic

浏览器本地批量图片压缩工具：文件不出本机，WASM 编码，实时细节预览。部署为 Netlify 静态站。

## Language

**作业（Job）**:
一个输入文件连同它的参数与产物的完整处理单元。
UI 对用户呈现为「图片」（标签页、列表标题、应用范围），模型与代码层仍称 Job。
_Avoid_: 任务、图片项

**参数（Params）**:
挂在单个作业上的压缩设置（格式／质量或力度／长边／保留 EXIF）。
_Avoid_: 设置、配置

**全局默认（Global Default）**:
新导入作业继承的参数快照；"应用到全部"会把它覆写到所有作业。
_Avoid_: 预设

**格式（Format）**:
输出编码格式：JPEG / WebP / AVIF / PNG 之一。

**质量（Quality）**:
0-100 的有损压缩强度，仅 JPEG / WebP / AVIF 有此参数。
_Avoid_: 清晰度

**力度（Effort）**:
PNG 无损优化的等级（1-6），PNG 没有"质量"。
_Avoid_: 压缩率

**长边（Long Edge）**:
等比缩放的目标像素，只缩不放；空值 = 原尺寸。
_Avoid_: 分辨率、宽度

**快捷档位（Preset）**:
长边的常用取值：1/2、1/4，以及 384 网格值（384×n，1920 以上取 768 的倍数）。
_Avoid_: 模板、默认值

**细节预览（Preview）**:
选中作业的分屏 1:1 对比视图，所见即所得。
_Avoid_: 缩略图

**裁剪预览（Crop Preview）**:
只对当前可见区域跑同参数管线的低延迟预览路径。
_Avoid_: 实时压缩

**全量编码（Full Encode）**:
后台队列对整图执行的编码，产出真实体积与产物。
_Avoid_: 导出

**产物（Artifact）**:
全量编码输出的可下载文件，命名 `{原名}.{新扩展名}`。

**节省率（Savings）**:
（原体积 − 产物体积）／原体积。

**元数据剥离（Metadata Stripping）**:
默认抹除 EXIF 等全部元数据（含 GPS），方向信息烘进像素；仅 JPEG 输出可 opt-in 保留。

## Relationships

- 一个 **作业** 持有恰好一份 **参数**；参数变更只重跑该作业
- **裁剪预览** 与 **全量编码** 必须走完全相同的参数管线，否则预览失真
- 一个作业每次 **全量编码** 产出一个 **产物**
- **质量** 与 **力度** 互斥，由 **格式** 决定哪个生效
- **元数据剥离** 对所有格式默认生效；保留仅适用于 JPEG 输出

## Example dialogue

> **Dev:** "用户改了某个**作业**的**质量**，其他作业要重跑吗？"
> **领域专家:** "不重跑。只有'应用到全部'才把新参数广播出去。"
> **Dev:** "那拖动质量滑块时**细节预览**显示的是**产物**吗？"
> **领域专家:** "不是产物，是**裁剪预览**——同一根管线跑出来的局部结果，视觉上与产物一致；真实体积以**全量编码**为准。"

## Flagged ambiguities

- "预览"曾被同时用于列表缩略图和 1:1 对比视图 — 已解决：前者叫缩略图，**细节预览** 专指后者
- "质量"曾被用于 PNG — 已解决：PNG 只有 **力度**，质量是有损格式专属概念
